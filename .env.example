# Environment variables for the BK-tree search application

#############################
# Local development
#############################

# Path to MRCONSO-like terms file (local development)
TERMS_PATH=data/mrconso_sample.txt

# Application environment (dev, staging, prod)
APP_ENV=dev

# Server configuration
PORT=8080

# Enable Python baseline in local dev and disable artifact for /search/python
ENABLE_PYTHON_BASELINE=true
AUTO_LOAD_ON_STARTUP=true
MRCONSO_PATH=data/umls/2025AA/MRCONSO.RRF
MRCONSO_FORMAT=rrf
MAX_TERMS=50000
BKTREE_ARTIFACT_PATH=

# Optional: prefer RAM-backed temp if available
# BK_TMP_DIR=/dev/shm

# No auto-shutdown during local runs
SHUTDOWN_AFTER_SECONDS=

# Logging
DEPLOY_ENV=dev
LOG_LEVEL=INFO

#############################
# Cloud Run deployment (set via Actions secrets/vars)
#############################

# GCP_PROJECT_ID=<your-gcp-project-id>
# GCP_REGION=<your-cloud-run-region>
# SERVICE_NAME=<your-cloud-run-service-name>
# GCP_ARTIFACT_REPO=<region>-docker.pkg.dev/<project-id>/<repo-name>
# GCP_SA_EMAIL=<service-account>@<project-id>.iam.gserviceaccount.com
# GCP_WORKLOAD_IDENTITY_PROVIDER=projects/<project-number>/locations/global/workloadIdentityPools/<pool>/providers/<provider>

# STAGING (for Python vs C++ comparison on Cloud Run):
#   ENABLE_PYTHON_BASELINE=true
#   AUTO_LOAD_ON_STARTUP=true
#   MRCONSO_PATH=gs://<bucket>/umls/2025AA/MRCONSO.RRF
#   MRCONSO_FORMAT=rrf
#   MAX_TERMS=50000
#   BKTREE_ARTIFACT_PATH=

# PRODUCTION (fastest; BK-tree only):
#   ENABLE_PYTHON_BASELINE=false
#   AUTO_LOAD_ON_STARTUP=true
#   BKTREE_ARTIFACT_PATH=gs://<bucket>/cache/mrconso_bktree.tar.gz
#   MRCONSO_FORMAT=rrf
