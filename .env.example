# Environment variables for the BK-tree search application

# Path to MRCONSO-like terms file (local development)
TERMS_PATH=data/mrconso_sample.txt

# Application environment (dev, staging, prod)
APP_ENV=dev

# Server configuration
PORT=8080

# Deployment metadata
DEPLOY_ENV=dev
LOG_LEVEL=INFO

# GCP Cloud Run configuration (for deployment)
# Uncomment and populate when deploying
# GCP_PROJECT_ID=<your-gcp-project-id>
# GCP_REGION=<your-cloud-run-region>
# SERVICE_NAME=<your-cloud-run-service-name>
# GCP_ARTIFACT_REPO=<region>-docker.pkg.dev/<project-id>/<repo-name>
# GCP_SA_EMAIL=<service-account>@<project-id>.iam.gserviceaccount.com
# GCP_WORKLOAD_IDENTITY_PROVIDER=projects/<project-number>/locations/global/workloadIdentityPools/<pool>/providers/<provider>
# MRCONSO_PATH=gs://<bucket>/umls/2025AA/MRCONSO.RRF
